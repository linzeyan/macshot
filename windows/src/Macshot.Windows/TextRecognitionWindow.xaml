<Window
    x:Class="Macshot.Windows.TextRecognitionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Recognized text">
    <Grid
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
        Padding="16"
        RowDefinitions="*,Auto,Auto"
        RowSpacing="12">
        <!-- Editable on purpose: OCR misreads characters, and fixing a wrong letter
             before copying is faster than retyping the line somewhere else. -->
        <TextBox
            x:Name="RecognizedTextBox"
            AcceptsReturn="True"
            TextWrapping="Wrap"
            IsSpellCheckEnabled="False"
            ScrollViewer.VerticalScrollBarVisibility="Auto" />

        <!-- Collapsed in the offline build, where the code behind the button does not
             exist at all. See TranslationService. -->
        <StackPanel
            x:Name="TranslateRow"
            Grid.Row="1"
            Orientation="Horizontal"
            Spacing="8">
            <ComboBox
                x:Name="TargetLanguageBox"
                MinWidth="200"
                SelectionChanged="TargetLanguage_Changed" />
            <Button x:Name="TranslateButton" Content="Translate" Click="Translate_Click" />
        </StackPanel>

        <StackPanel
            Grid.Row="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Spacing="8">
            <TextBlock
                x:Name="StatusText"
                VerticalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <Button Content="Close" Click="Close_Click" />
            <Button Content="Copy" Click="Copy_Click" Style="{StaticResource AccentButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>
